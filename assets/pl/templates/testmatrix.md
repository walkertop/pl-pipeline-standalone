# TestMatrix: [Change 名称]

> **模板版本**: v1.0
> **用途**: 在 PLAN 阶段定义测试矩阵（场景 × 断言），作为 VERIFY/OBSERVE 的验证依据
> **输入**: `spec.md`（功能清单）+ `api.md`（错误码）
> **消费者**: `migration-guardian`（执行验证）、`pl-migration-verify.sh`
> **位置**: `pl/changes/<change-id>/testmatrix.md`

---

## 1. 测试覆盖总览

| 维度 | 计划覆盖 | 实际覆盖 |
|------|---------|---------|
| 功能场景（P0/P1） | N | 0 |
| 状态机路径 | N | 0 |
| 错误分支 | N | 0 |
| 埋点事件 | N | 0 |

---

## 2. 场景 × 断言矩阵

### 2.1 主场景（正常路径）

| 场景 ID | 场景描述 | 前置条件 | 操作步骤 | 断言 | 对应 F# | 状态 |
|--------|---------|---------|---------|------|---------|------|
| S1 | 页面正常加载 | 登录态 + 有数据 | 进入页面 | UI 展示数据；PV 上报 | F1 | ⬜ TODO |
| S2 | 按钮点击成功 | S1 完成 | 点击"确认" | 接口返回成功；跳转下一页；Click 上报 | F4 | ⬜ TODO |

### 2.2 状态机路径

| 场景 ID | 起始状态 | 操作 | 终止状态 | 断言 | 对应 F# |
|--------|---------|------|---------|------|---------|
| SM1 | 待支付 | 支付成功 | 已支付 | UI 切换 + Toast | F2 |
| SM2 | 待支付 | 取消 | 已取消 | 跳回列表 | F2 |

### 2.3 错误分支

| 场景 ID | 错误类型 | 模拟方式 | 期望行为 | 断言 | 对应错误码 |
|--------|---------|---------|---------|------|-----------|
| E1 | 网络超时 | ApiSelfCheck → Mock timeout | 显示网络错误提示 + 重试按钮 | Toast 出现 | - |
| E2 | 接口 ret != 0 | Mock ret=-1 | 显示业务错误提示 | Toast 出现 msg 内容 | ret=-1 |
| E3 | 字段解析失败 | Mock 返回异常类型 | 宽松解析降级 + 默认值展示 | 页面不白屏 | - |
| E4 | 登录失效 | Mock ret=301 | 跳转登录页 | 路由到 Login | ret=301 |

### 2.4 埋点验证

| 事件 | 触发条件 | 旧版 eventId | 新版实现位置 | 对应 F# | 状态 |
|------|---------|-------------|-------------|---------|------|
| PV | 页面曝光 | `page_view_xxx` | `appear()` | - | ⬜ TODO |
| Click | 按钮点击 | `btn_click_xxx` | onClick | F4 | ⬜ TODO |
| Expose | 卡片曝光 | `card_expose_xxx` | 列表滚动 | F2 | ⬜ TODO |

### 2.5 性能断言（OBSERVE 阶段）

| 指标 | 目标 | 测量方式 | 状态 |
|------|------|---------|------|
| 首屏加载 | < 1000ms (WiFi) | logcat + 埋点 | ⬜ TODO |
| 列表滚动 | 60fps | 性能面板录制 | ⬜ TODO |
| 内存峰值 | < 200MB | Profiler | ⬜ TODO |

---

## 3. 验收通过条件（VERIFY 门禁）

- [ ] 所有 P0 场景（S*）状态 ✅ PASS
- [ ] 所有状态机路径（SM*）状态 ✅ PASS
- [ ] 所有错误分支（E*）处理正确（不白屏、不崩溃）
- [ ] 所有 P0 埋点事件都能被 Logcat 捕获
- [ ] 性能指标达标（OBSERVE 阶段补充）

---

## 4. 测试工具与依赖

| 工具 | 用途 | 脚本入口 |
|------|------|---------|
| 静态分析 | 静态检查 | `$LINT_CMD`（由 adapter 注入，如 `scripts/lint.sh` / `npm run lint` / `ruff check`） |
| 编译工具 | 编译验证 | `$PL_BUILD_CHECK_CMD`（由 adapter 注入） |
| ApiSelfCheck | Mock 降级测试 | 项目自定义 API 自检实现 |
| Logcat Analyzer | 运行时日志分析 | `scripts/migration-logcat-analyzer.sh` |
| Auto-Test | UI 截图对比 | `scripts/auto-test-runner.sh` |

---

## 5. 变更记录

| 日期 | 变更 | 原因 |
|------|------|------|
| YYYY-MM-DD | 初始版本 | PLAN 阶段启动 |
